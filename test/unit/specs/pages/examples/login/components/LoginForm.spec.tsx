import { describe, it, expect } from 'vitest';
import LoginForm from '../../../../../../../src/pages/examples/login/components/LoginForm';

describe('LoginForm', () => {
  it('should render', () => {
    expect(LoginForm).toBeDefined();
  });
});
