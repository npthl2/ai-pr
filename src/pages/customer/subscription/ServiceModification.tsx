import { Box, Typography } from '@mui/material';

const ServiceModification = () => {
  return (
    <Box p={3}>
      <Typography variant='h4'>요금제/부가서비스 변경</Typography>
      {/* 요금제/부가서비스 변경 폼 컴포넌트들 추가 예정 */}
    </Box>
  );
};

export default ServiceModification;
